'''
On terminal With help of pip command we install external modules.
E.g: pip install pandas

import helps to import modules in our program:
'''

import math

r = float(input("Enter radius of circle: "))

area = math.pi * (r * r);

print("Area of circle is : ",area)

